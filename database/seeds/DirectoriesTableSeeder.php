<?php

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;

class DirectoriesTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // factory(App\Directory::class, 10)->create()->each(function ($directory) {
        //     $directory->issues()->save(factory(App\Issues::class)->make());
        // });
    }
}
